<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finix.kisiiattend">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KisiiAttend">
        <activity android:name=".UnitRecyclerviewActivity"></activity>
        <activity android:name=".UnitReportActivity" />
        <activity android:name=".DateSearchActivity" />
        <activity android:name=".EligibleActivity" />
        <activity android:name=".RegSearchActivity" />
        <activity android:name=".ReportsActivity" />
        <activity android:name=".NoEligibleActivity" />
        <activity android:name=".OveralAttendanceActivity" />
        <activity android:name=".UnitDetailsActivity" />
        <activity android:name=".AddUnitStudent" />
        <activity android:name=".AddUnitLecturer" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SelectOccupation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>